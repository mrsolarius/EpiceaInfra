---
# ========================================
# Handlers: common
# ========================================

- name: Reboot system
  ansible.builtin.reboot:
    msg: "Reboot pour appliquer drivers NVIDIA"
    post_reboot_delay: 30
  listen: Reboot system
  when: allow_automatic_reboot | default(false)
