---
# ========================================
# Role: common
# Description: Configuration système de base
# ========================================

- name: Mise à jour du système
  ansible.builtin.apt:
    upgrade: dist
    update_cache: true
    cache_valid_time: 3600

- name: Installation des paquets de base
  ansible.builtin.apt:
    name:
      - curl
      - wget
      - git
      - vim
      - htop
      - ncdu
      - tree
      - jq
      - unzip
      - ca-certificates
      - gnupg
      - lsb-release
      - ufw
      - fail2ban
      - nfs-common
      - prometheus-node-exporter
      - smartmontools
    state: present

- name: Configuration du service Node Exporter
  ansible.builtin.systemd:
    name: prometheus-node-exporter
    state: started
    enabled: true

- name: Configuration du timezone
  community.general.timezone:
    name: "{{ timezone }}"

- name: Configuration UFW - règles de base
  community.general.ufw:
    rule: "{{ item.rule }}"
    port: "{{ item.port }}"
    proto: "{{ item.proto | default('tcp') }}"
    comment: "{{ item.comment | default('') }}"
  loop:
    - {rule: 'allow', port: '22', comment: 'SSH'}
    - {rule: 'allow', port: '80', comment: 'HTTP'}
    - {rule: 'allow', port: '443', comment: 'HTTPS'}

- name: Configuration UFW - autoriser Node Exporter depuis le réseau local et Docker
  community.general.ufw:
    rule: allow
    port: '9100'
    proto: tcp
    from_ip: "{{ item }}"
    comment: 'Allow Node Exporter'
  loop:
    - '192.168.1.0/24'
    - '172.16.0.0/12'

- name: Configuration UFW - bloquer accès externe PostgreSQL, Redis et Node Exporter
  community.general.ufw:
    rule: deny
    port: "{{ item.port }}"
    proto: tcp
    comment: "{{ item.comment }}"
  loop:
    - {port: '5432', comment: 'Block external PostgreSQL'}
    - {port: '6379', comment: 'Block external Redis'}
    - {port: '9100', comment: 'Block external Node Exporter'}

- name: Configuration UFW - règles additionnelles (production)
  community.general.ufw:
    rule: "{{ item.rule }}"
    port: "{{ item.port | default(omit) }}"
    proto: "{{ item.proto | default('tcp') }}"
    from_ip: "{{ item.from | default(omit) }}"
    comment: "{{ item.comment | default('') }}"
  loop: "{{ ufw_additional_rules | default([]) }}"
  when: deploy_environment == 'production'

- name: Activation UFW
  community.general.ufw:
    state: enabled

- name: Configuration fail2ban
  ansible.builtin.systemd:
    name: fail2ban
    state: started
    enabled: true
  when: fail2ban_enabled | default(true)

- name: Configuration DNS local (test uniquement)
  ansible.builtin.lineinfile:
    path: /etc/hosts
    line: "127.0.0.1 {{ item.value }}"
    state: present
  loop: "{{ service_domains | dict2items }}"
  when: deploy_environment == 'test'

- name: Installer drivers NVIDIA (production avec GPU)
  ansible.builtin.apt:
    name:
      - nvidia-driver-{{ gpu_driver_version }}
      - nvidia-container-toolkit
    state: present
    update_cache: true
  when:
    - deploy_environment == 'production'
    - system_enable_gpu | bool
  notify: Reboot system

- name: Vérifier la présence de la clé privée SSH
  ansible.builtin.stat:
    path: "/home/{{ common_system_user }}/.ssh/id_rsa"
  register: ssh_private_key

- name: Vérifier les permissions des clés privées SSH (si existantes)
  ansible.builtin.file:
    path: "/home/{{ common_system_user }}/.ssh/id_rsa"
    mode: '0600'
    state: file
  when: ssh_private_key.stat.exists | default(false)

- name: Afficher résumé configuration
  ansible.builtin.debug:
    msg:
      - "âœ… Paquets de base installés"
      - "âœ… Firewall UFW configuré"
      - "âœ… Fail2ban activé"
      - "{% if deploy_environment == 'test' %}âœ… DNS local configuré{% endif %}"
      - "{% if system_enable_gpu %}âœ… Drivers NVIDIA installés{% endif %}"
